/**
 * Centralized icon imports for better tree-shaking and bundle optimization
 * This file imports only the icons we actually use, reducing bundle size
 */

// Navigation icons
export { ChevronLeft, ChevronRight, ChevronDown, ChevronUp, Home, MoreHorizontal } from 'lucide-react';

// Action icons
export { Check, X, RefreshCw, Download, Filter, Search, SortAsc, SortDesc, Star, FileText, FileSpreadsheet } from 'lucide-react';

// Status icons
export { CheckCircle, XCircle, AlertCircle, AlertTriangle, Info, Clock } from 'lucide-react';

// UI icons
export { HelpCircle, Keyboard, ExternalLink, Copy, QrCode } from 'lucide-react';

// Chart icons
export { BarChart3, TrendingUp, MapPin, Users, Activity } from 'lucide-react';

// Form icons
export { User, Phone, Mail, Calendar, Clock as ClockIcon } from 'lucide-react';
